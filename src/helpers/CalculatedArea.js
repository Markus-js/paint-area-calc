// import { useContext } from "react";
// import { AppContext } from "../context/states";

// const CalculatedArea = () => {
//   const { result, selectedProduct, setCalculatedArea, selectedValue } =
//     useContext(AppContext);

//   if (selectedProduct) {
//     // Calc ( l * h / sum of dækkeevne * lag )

//     return (
//       (result / selectedProduct.fields.dkkeevne) *
//       selectedProduct.fields.lag
//     ).toFixed(2);
//     // ((25 / 6.5) * 2).toFixed(2)
//   }
// };
// export default CalculatedArea;
